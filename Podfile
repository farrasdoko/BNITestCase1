# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestFarras' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestFarras
  pod 'RealmSwift', '~>10'

  target 'TestFarrasTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestFarrasUITests' do
    # Pods for testing
  end

end
