//
//  BNIUserDefaultManager.swift
//  TestFarras
//
//  Created by Farras on 19/09/23.
//

import Foundation

// TODO: Delete this.
/*
struct UserDefaultManager {
    private static var BALANCE_KEY = "wallet"
    static var shared = UserDefaultManager()
    var balance: Int? {
        get {
            return UserDefaults.standard.integer(forKey: UserDefaultManager.BALANCE_KEY)
        }
        set {
            if newValue == nil {
                UserDefaults.standard.removeObject(forKey: UserDefaultManager.BALANCE_KEY)
                return
            }
            UserDefaults.standard.set(newValue, forKey: UserDefaultManager.BALANCE_KEY)
            
            // Notify that balance has new value.
            NotificationCenter.default.post(name: NSNotification.Name.BalanceDidChange, object: nil)
        }
    }
}
*/
